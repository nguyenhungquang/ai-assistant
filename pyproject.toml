[project]
name = "obsidian-research-vault-agent"
version = "0.1.0"
description = "Local-first research vault agent foundation"
requires-python = ">=3.12"
dependencies = [
  "beautifulsoup4>=4.12.3",
]

[tool.uv]
package = false
